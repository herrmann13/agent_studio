# MVP Scope

The first release discovers local agents and skills, tracks selected projects, and previews their association with agents and projects.

Skills can be copied by explicit drag-and-drop between global, agent, and project locations. Deletion is available only from the context menu, requires confirmation, and creates a local backup first.

The application will not intercept terminal input, run agents, execute skill scripts, or write configuration automatically.
